<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>LumenNyx Studios – Model Portal Login</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="auth-page">

  <!-- Site Header -->
  <header class="site-header">
    <div class="site-header-inner">
      <a href="/" class="brand-link">
        <span class="brand-mark">
          <img src="/img/lumennyx-mark.png" alt="LumenNyx Studios logo">
        </span>
        <span class="brand-text">LUMENNYX STUDIOS</span>
      </a>

      <nav class="main-nav">
        <a href="/">Home</a>
        <a href="/login" class="active">Login</a>
        <a href="/register">Register</a>
      </nav>
    </div>
  </header>

  <main class="page-wrapper">

    <div class="auth-card">
      <h2>Model & Staff Login</h2>
      <p class="auth-subtext">
        Sign in to your LumenNyx Studios portal account to manage your profile,
        documents, and consent settings.
      </p>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert error"><%= error %></div>
      <% } %>

      <% if (typeof message !== 'undefined' && message) { %>
        <div class="alert success"><%= message %></div>
      <% } %>

      <form action="/login" method="POST" class="auth-form">
        <label>
          Username
          <input
            type="text"
            name="username"
            autocomplete="username"
            required
          />
        </label>

        <label>
          Password
          <input
            type="password"
            name="password"
            autocomplete="current-password"
            required
          />
        </label>

        <button type="submit" class="btn primary" style="margin-top:0.4rem;">
          Log In
        </button>
      </form>

      <div class="auth-footer">
        <p>
          Usernames are <strong>not case-sensitive</strong> – you can log in with
          any capitalization.
        </p>
        <p style="margin-top:0.5rem;">
          Don’t have an account yet?
          <a href="/register">Request / create a portal account</a>.
        </p>

        <p class="auth-note">
          This private portal is for LumenNyx Studios models and approved staff only.
          If you received this link in error, please do not attempt to access it.
        </p>
      </div>
    </div>

  </main>

</body>
</html>
