<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>LumenNyx Studios – Portal Login</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="auth-page">

  <!-- Site Header -->
  <header class="site-header">
    <div class="site-header-inner">
      <a href="/" class="brand-link">
        <span class="brand-mark">
          <img src="/img/lumennyx-mark.png" alt="LumenNyx Studios logo">
        </span>
        <span class="brand-text">LUMENNYX STUDIOS</span>
      </a>

      <nav class="main-nav">
        <a href="/">Home</a>
        <a href="/login" class="active">Model Login</a>
        <a href="/register">Register</a>
        <a href="/staff-login">Staff Login</a>
      </nav>
    </div>
  </header>

  <main class="page-wrapper">

    <div class="auth-card">
      <h2>Model Login</h2>
      <p class="auth-subtext">
        Sign in to manage your profile, documents, and onboarding workflow.
        Staff should use the dedicated <a href="/staff-login">Staff Login</a>.
      </p>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert error"><%= error %></div>
      <% } %>

      <% if (typeof message !== 'undefined' && message) { %>
        <div class="alert success"><%= message %></div>
      <% } %>

      <form action="/login" method="POST" class="auth-form">
        <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <% } %>

        <label>
          Username
          <input
            type="text"
            name="username"
            autocomplete="username"
            required
          />
        </label>

        <label>
          Password
          <input
            type="password"
            name="password"
            autocomplete="current-password"
            required
          />
        </label>

        <button type="submit" class="btn primary" style="margin-top:0.4rem;">
          Log In
        </button>
      </form>

      <div class="auth-footer">
        <p>
          Usernames are <strong>not case-sensitive</strong>.
        </p>

        <p style="margin-top:0.5rem;">
          Don’t have an account yet?
          <a href="/register">Submit a model application</a>.
        </p>

        <p class="auth-note">
          This private portal is for LumenNyx Studios models and approved staff only.
          If you received this link in error, please do not attempt to access it.
        </p>
      </div>
    </div>

  </main>

</body>
</html>
