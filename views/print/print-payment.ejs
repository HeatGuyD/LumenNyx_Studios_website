<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Executed — Payment</title>
  <style>
    body{font-family:Arial, sans-serif; color:#111; font-size:12px}
    h1{font-size:18px; margin:0 0 6px}
    .meta{margin:0 0 14px; color:#444}
    .box{border:1px solid #333; padding:10px; border-radius:8px}
    .check{font-weight:bold}
    .sigWrap{margin-top:14px}
    .hr{border-top:1px solid #ddd; margin:14px 0}
  </style>
</head>
<body>
  <h1>Executed: Payment Confirmation / Receipt</h1>
  <div class="meta">Portal Document: <strong><%= payload.docType %></strong></div>

  <div class="box">
    <p><strong>Amount:</strong> <%= payload.amount %></p>
    <p><strong>Method:</strong> <%= payload.method %></p>
    <p><strong>Date:</strong> <%= payload.date %></p>
    <p><span class="check"><%= payload.ack_payment_correct ? "☑" : "☐" %></span> Performer acknowledges payment details are correct</p>
    <div class="hr"></div>
    <p><strong>Notes:</strong> <%= payload.notes || "—" %></p>
  </div>

  <div class="sigWrap">
    <strong>Signature</strong><br/>
    <% if (signature && signature.signature_data_url) { %>
      <img src="<%= signature.signature_data_url %>" style="height:55px" />
    <% } else { %>
      <em>No signature image</em>
    <% } %>
    <div><strong>Date/Time (ISO):</strong> <%= audit.signedAtIso %></div>

    <div class="hr"></div>
    <strong>Audit Trail</strong><br/>
    IP: <%= audit.ip %><br/>
    User Agent: <%= audit.ua %><br/>
  </div>
</body>
</html>
